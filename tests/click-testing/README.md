# Click Testing (Pytest + CliRunner)

Minimal tests that exercise the CLI via Click’s `CliRunner` without building a binary.

## Prerequisites

- Python 3.13+ and pip
- Install deps: `pip install -r requirements.txt`

## Run the tests

- Go to home directory: `cd ../..`
- `python -m pytest tests/click-testing/`
